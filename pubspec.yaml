name: dashboard_mahasiswa
description: Dashboard Mahasiswa D4TI Flutter App

publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  dio: ^5.4.0

  # ✅ Hanya gunakan Riverpod, HAPUS package 'provider' untuk menghindari konflik
  flutter_riverpod: ^2.5.1

  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
